// Campaign functions

export const GMMessage = (msg) => {
  // Send GM a message
    return true;
}
export const GiveItem = (player, Item) => {
  // Send a specific player an item from your inventory
  return true;
}
export const PushStatus = (status) => {
  // Send status update to other players
  return true;
}
export const ReceivedStatus = (status) => {
  // Receive a status update (containing player id) from other players
  return true;
}
